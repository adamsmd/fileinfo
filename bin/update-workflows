#!/bin/bash

set -ex
for i in default docker multiarch shell windows windows-shell; do
  yq e 'explode(.)' \
    <(cat .github/docker-steps.src.yaml .github/"$i".src.yaml) \
    >.github/workflows/"$i".yaml
done
